"""
Internal pydantic utilities that should be excluded by smart filtering.
This file simulates the internal implementation files that users typically
don't need to see in code intelligence.
"""

from typing import Any, Dict


def _internal_validation_function(data: Any) -> bool:
    """Internal validation function."""
    return True


def _parse_field_info(field_info: Dict[str, Any]) -> Dict[str, Any]:
    """Internal field parsing."""
    return field_info


class _InternalValidator:
    """Internal validator class."""
    
    def __init__(self):
        self._rules = []
    
    def _add_rule(self, rule: Any) -> None:
        """Add internal validation rule."""
        self._rules.append(rule) 